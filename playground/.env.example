# No runtime configuration is required.
#
# The showcase queries the Verana Trust Resolver directly from the browser
# (its CORS is open), so there is no backend/proxy and no secrets. The resolver,
# Inji Verify and ecosystem details are build-time constants in app/config.ts.
